[project]
name = "smart-expense-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.0",
]

[dependency-groups]
dev = [
    "httpx2>=2.9.1",
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
